<Window x:Class="TurFirma.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход в систему" Height="460" Width="520" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Grid Background="{StaticResource SurfaceBrush}">
        <Border Style="{StaticResource CardStyle}" Width="420" Height="320" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="26">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Вход в аккаунт" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,6"/>
                <TextBlock Grid.Row="1" Text="Введите email и пароль" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,14"/>

                <TextBox Grid.Row="2" Width="360" Height="44" HorizontalAlignment="Left"
                         Text="{Binding Auth.Email, UpdateSourceTrigger=PropertyChanged}" Tag="Email"/>

                <PasswordBox x:Name="PasswordInput" Grid.Row="3" Width="360" Height="44" HorizontalAlignment="Left" Margin="6"/>

                <StackPanel Grid.Row="4" Margin="0,12,0,0">
                    <Button Width="360" Height="44" Style="{StaticResource PrimaryButtonStyle}" Content="Войти" Click="Login_Click"/>
                    <TextBlock Text="{Binding Auth.Status}" Foreground="#B14444" Margin="6,8,0,0" TextWrapping="Wrap"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
