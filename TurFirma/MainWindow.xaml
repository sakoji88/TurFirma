<Window x:Class="TurFirma.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:TurFirma.ViewModels"
        xmlns:models="clr-namespace:TurFirma.Models"
        mc:Ignorable="d"
        Title="TurFirma — Travel Management" Height="900" Width="1320" MinHeight="760" MinWidth="1100">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Background="#20284A" Padding="16">
            <DockPanel>
                <StackPanel DockPanel.Dock="Bottom" Margin="0,24,0,0">
                    <TextBlock Text="TurFirma" Foreground="White" FontWeight="SemiBold" FontSize="18"/>
                    <TextBlock Text="Tour Booking System" Foreground="#B7C3EA" FontSize="12"/>
                </StackPanel>

                <StackPanel>
                    <TextBlock Text="Навигация" Foreground="#95A4D7" Margin="8" FontSize="12"/>
                    <Button Style="{StaticResource SidebarButtonStyle}" Command="{Binding NavigateCommand}" CommandParameter="Auth" Content="&#xE77B;  Авторизация" FontFamily="Segoe MDL2 Assets"/>
                    <Button Style="{StaticResource SidebarButtonStyle}" Command="{Binding NavigateCommand}" CommandParameter="Catalog" Content="&#xE14C;  Каталог туров" FontFamily="Segoe MDL2 Assets"/>
                    <Button Style="{StaticResource SidebarButtonStyle}" Command="{Binding NavigateCommand}" CommandParameter="Cart" Content="&#xE719;  Корзина" FontFamily="Segoe MDL2 Assets"/>
                    <Button Style="{StaticResource SidebarButtonStyle}" Command="{Binding NavigateCommand}" CommandParameter="Profile" Content="&#xE13D;  Личный кабинет" FontFamily="Segoe MDL2 Assets"/>
                    <Button Style="{StaticResource SidebarButtonStyle}" Command="{Binding NavigateCommand}" CommandParameter="Admin" Content="&#xE713;  Админ-панель" FontFamily="Segoe MDL2 Assets"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Content -->
        <Grid Grid.Column="1" Margin="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Style="{StaticResource CardStyle}" Padding="18" Margin="0,0,0,14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Система управления турами" FontSize="28" FontWeight="SemiBold"/>
                        <TextBlock Text="Поиск, бронь, оплата и подтверждение поездок в едином интерфейсе" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>
                    <Border Grid.Column="1" Background="#EEF1FF" CornerRadius="10" Padding="14,8">
                        <TextBlock Text="{Binding CurrentSection}" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"/>
                    </Border>
                </Grid>
            </Border>

            <Grid Grid.Row="1">
                <!-- AUTH -->
                <Border>
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource CardStyle}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentSection}" Value="Auth">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Регистрация / Авторизация" FontSize="24" FontWeight="SemiBold"/>
                        <Grid Grid.Row="1" Margin="0,16,0,0" HorizontalAlignment="Center" MaxWidth="760">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Личные данные" FontWeight="SemiBold" Margin="6"/>
                                <TextBox Text="{Binding Auth.FullName, UpdateSourceTrigger=PropertyChanged}" Tag="ФИО"/>
                                <TextBox Text="{Binding Auth.Email, UpdateSourceTrigger=PropertyChanged}" Tag="Email"/>
                                <TextBox Text="{Binding Auth.Phone, UpdateSourceTrigger=PropertyChanged}" Tag="Телефон"/>
                                <TextBox Text="{Binding Auth.Password, UpdateSourceTrigger=PropertyChanged}" Tag="Пароль"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Паспортные данные" FontWeight="SemiBold" Margin="6"/>
                                <TextBox Text="{Binding Auth.PassportSeries, UpdateSourceTrigger=PropertyChanged}" Tag="Серия"/>
                                <TextBox Text="{Binding Auth.PassportNumber, UpdateSourceTrigger=PropertyChanged}" Tag="Номер"/>
                                <DatePicker SelectedDate="{Binding Auth.PassportIssueDate}"/>
                                <StackPanel Orientation="Horizontal" Margin="4,18,0,0">
                                    <Button Style="{StaticResource PrimaryButtonStyle}" Content="Зарегистрироваться" Command="{Binding Auth.RegisterCommand}"/>
                                    <Button Style="{StaticResource PrimaryButtonStyle}" Background="{StaticResource AccentBrush}" Content="Войти" Command="{Binding Auth.LoginCommand}"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                        <TextBlock Grid.Row="2" Text="{Binding Auth.Status}" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" Margin="6,16,0,0"/>
                    </Grid>
                </Border>

                <!-- CATALOG -->
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Border>
                        <Border.Style>
                            <Style TargetType="Border" BasedOn="{StaticResource CardStyle}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentSection}" Value="Catalog">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="Каталог туров" FontSize="24" FontWeight="SemiBold"/>
                            <WrapPanel Grid.Row="1" Margin="0,14,0,8">
                                <TextBox Width="170" Tag="Направление" Text="{Binding Catalog.Destination, UpdateSourceTrigger=PropertyChanged}"/>
                                <DatePicker SelectedDate="{Binding Catalog.FromDate}"/>
                                <DatePicker SelectedDate="{Binding Catalog.ToDate}"/>
                                <TextBox Width="130" Tag="Цена до" Text="{Binding Catalog.MaxPrice, UpdateSourceTrigger=PropertyChanged}"/>
                                <ComboBox Width="180" SelectedValue="{Binding Catalog.TourType, UpdateSourceTrigger=PropertyChanged}">
                                    <ComboBoxItem Content=""/>
                                    <ComboBoxItem Content="Пляжный"/>
                                    <ComboBoxItem Content="Экскурсионный"/>
                                    <ComboBoxItem Content="Активный"/>
                                </ComboBox>
                                <TextBox Width="110" Tag="Мест" Text="{Binding Catalog.Seats, UpdateSourceTrigger=PropertyChanged}"/>
                                <CheckBox Margin="12,12,0,0" Content="Страховка" IsChecked="{Binding Catalog.InsuranceSelected}"/>
                                <CheckBox Margin="12,12,0,0" Content="Трансфер" IsChecked="{Binding Catalog.TransferSelected}"/>
                                <Button Style="{StaticResource PrimaryButtonStyle}" Content="Найти туры" Command="{Binding Catalog.LoadToursCommand}"/>
                            </WrapPanel>

                            <ListBox Grid.Row="2" ItemsSource="{Binding Catalog.Tours}" SelectedItem="{Binding Catalog.SelectedTour}" BorderThickness="0" Background="Transparent">
                                <ListBox.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel/>
                                    </ItemsPanelTemplate>
                                </ListBox.ItemsPanel>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource CardStyle}" Width="280" Padding="0">
                                            <StackPanel>
                                                <Border Height="130" Background="#DDE3FF" CornerRadius="12,12,0,0">
                                                    <TextBlock Text="Фото тура" Foreground="#5969A6" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                                </Border>
                                                <StackPanel Margin="14">
                                                    <TextBlock Text="{Binding Destination}" FontWeight="SemiBold" FontSize="15" TextWrapping="Wrap"/>
                                                    <TextBlock Text="{Binding TourType}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,0"/>
                                                    <TextBlock Text="{Binding StartDate, StringFormat='с {0:dd.MM.yyyy}'}" Margin="0,10,0,0"/>
                                                    <TextBlock Text="{Binding EndDate, StringFormat='по {0:dd.MM.yyyy}'}"/>
                                                    <TextBlock Text="{Binding BasePrice, StringFormat={}{0:N0} ₽}" FontSize="20" FontWeight="Bold" Margin="0,8,0,0"/>
                                                    <Button Style="{StaticResource PrimaryButtonStyle}" Content="Забронировать" Command="{Binding DataContext.Catalog.BookCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </Border>
                </ScrollViewer>

                <!-- CART -->
                <Border>
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource CardStyle}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentSection}" Value="Cart">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <DockPanel>
                            <TextBlock Text="Корзина и оплата" FontSize="24" FontWeight="SemiBold" DockPanel.Dock="Left"/>
                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                                <Button Style="{StaticResource PrimaryButtonStyle}" Content="Обновить" Command="{Binding Cart.RefreshCommand}"/>
                                <ComboBox Width="170" SelectedItem="{Binding Cart.SelectedMethod}">
                                    <x:Static Member="models:PaymentMethod.Card"/>
                                    <x:Static Member="models:PaymentMethod.EWallet"/>
                                </ComboBox>
                                <Button Style="{StaticResource PrimaryButtonStyle}" Background="{StaticResource AccentBrush}" Content="Оплатить" Command="{Binding Cart.PayCommand}"/>
                            </StackPanel>
                        </DockPanel>

                        <DataGrid Grid.Row="1" ItemsSource="{Binding Cart.NewBookings}" SelectedItem="{Binding Cart.SelectedBooking}" AutoGenerateColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Бронь" Binding="{Binding Id}" Width="70"/>
                                <DataGridTextColumn Header="Направление" Binding="{Binding Tour.Destination}" Width="*"/>
                                <DataGridTextColumn Header="Мест" Binding="{Binding Seats}" Width="80"/>
                                <DataGridTextColumn Header="Сумма" Binding="{Binding TotalPrice, StringFormat={}{0:N0} ₽}" Width="140"/>
                                <DataGridTextColumn Header="Резерв до" Binding="{Binding ReservedUntilUtc, StringFormat=dd.MM.yyyy HH:mm}" Width="180"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <Border Grid.Row="2" Background="#EFF2FF" CornerRadius="10" Padding="14" Margin="8">
                            <DockPanel>
                                <TextBlock Text="Итог к оплате:" FontWeight="SemiBold"/>
                                <TextBlock DockPanel.Dock="Right" Text="{Binding Cart.SelectedBooking.TotalPrice, StringFormat={}{0:N0} ₽}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"/>
                            </DockPanel>
                        </Border>
                    </Grid>
                </Border>

                <!-- PROFILE -->
                <Border>
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource CardStyle}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentSection}" Value="Profile">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <DockPanel>
                            <TextBlock Text="Личный кабинет" FontSize="24" FontWeight="SemiBold" DockPanel.Dock="Left"/>
                            <Button Style="{StaticResource PrimaryButtonStyle}" DockPanel.Dock="Right" Content="Обновить" Command="{Binding Profile.LoadBookingsCommand}"/>
                        </DockPanel>

                        <ItemsControl Grid.Row="1" ItemsSource="{Binding Profile.AllBookings}" Margin="0,14,0,0">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource CardStyle}" Padding="14" Margin="6">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock Text="{Binding Tour.Destination}" FontWeight="SemiBold" FontSize="16"/>
                                                <TextBlock Text="{Binding CreatedAtUtc, StringFormat='Создано: {0:dd.MM.yyyy HH:mm}'}" Foreground="{StaticResource TextSecondaryBrush}"/>
                                                <TextBlock Text="{Binding TotalPrice, StringFormat='Сумма: {0:N0} ₽'}" Margin="0,4,0,0"/>
                                            </StackPanel>
                                            <Border Grid.Column="1" CornerRadius="9" Padding="10,4" HorizontalAlignment="Right">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="#E4E9F9"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Status}" Value="Paid">
                                                                <Setter Property="Background" Value="#D9F7E7"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Cancelled">
                                                                <Setter Property="Background" Value="#FFE1E1"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding Status}" FontWeight="SemiBold"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </Border>

                <!-- ADMIN -->
                <Border>
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource CardStyle}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentSection}" Value="Admin">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Админ-панель менеджера" FontSize="24" FontWeight="SemiBold"/>
                        <StackPanel Orientation="Horizontal" Margin="0,14,0,10">
                            <Button Style="{StaticResource PrimaryButtonStyle}" Content="Загрузить" Command="{Binding Admin.LoadCommand}"/>
                            <ComboBox Width="220" ItemsSource="{Binding Admin.Guides}" SelectedItem="{Binding Admin.SelectedGuide}" DisplayMemberPath="FullName"/>
                            <ComboBox Width="220" ItemsSource="{Binding Admin.Transports}" SelectedItem="{Binding Admin.SelectedTransport}" DisplayMemberPath="Name"/>
                            <Button Style="{StaticResource PrimaryButtonStyle}" Background="{StaticResource AccentBrush}" Content="Подтвердить и назначить" Command="{Binding Admin.ConfirmCommand}"/>
                        </StackPanel>
                        <DataGrid Grid.Row="1" ItemsSource="{Binding Admin.PendingBookings}" SelectedItem="{Binding Admin.SelectedBooking}" AutoGenerateColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                                <DataGridTextColumn Header="Клиент" Binding="{Binding User.FullName}" Width="*"/>
                                <DataGridTextColumn Header="Тур" Binding="{Binding Tour.Destination}" Width="*"/>
                                <DataGridTextColumn Header="Мест" Binding="{Binding Seats}" Width="70"/>
                                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="130"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <TextBlock Grid.Row="2" Text="{Binding Admin.Status}" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="SemiBold" Margin="10"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>
